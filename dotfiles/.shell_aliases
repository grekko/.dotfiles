# Aliases shared across all systems and shells (bash/fish)
alias ssht="ssh-tmux"
alias ll="ls -halt"
alias be="bundle exec"
alias vi="nvim"
alias up!="dotfiles-up; source $DOTFILES_SHELL_CONFIG"

# ssh
alias hosts="sed -n 's/Host//p' ~/.ssh/config|grep -v Name"
