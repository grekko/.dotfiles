# Sends a null packet to the server each 3 mins
# Disconnecting if there was no response after 2 tries
Host *
  UseKeychain yes
  ServerAliveInterval 180
  ServerAliveCountMax 2
  Compression yes
  ServerAliveInterval 600
  ControlMaster auto
