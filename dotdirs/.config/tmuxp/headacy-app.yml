session_name: headacy-app
start_directory: ~/scm/tame-health.headacy-app

windows:
  - window_name: dev
    layout: ebd6,490x101,0,0[490x55,0,0{245x55,0,0,55,244x55,246,0,59},490x45,0,56{122x45,0,56,57,122x45,123,56,63,244x45,246,56,58}]
    panes:
      - shell_command:
        - pg_ctl start -l log/postgres.log
        - git up
        - bundle check; or bundle install
        - overmind start --procfile Procfile.dev
      - shell_command:
        - bundle exec rails c
      - shell_command:
        - nvim README.md
