session_name: ws
start_directory: ~/scm

windows:
  - window_name: Dev
    panes:
      - shell_command:
        - ssh-add --apple-load-keychain ~/.ssh/
        - brew update
        - brew cleanup
        - brew outdated
