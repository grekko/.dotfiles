session_name: kr
start_directory: ~/scm/krautreporter.krautreporter
windows:
  - window_name: Dev
    layout: 2c0c,490x101,0,0{245x101,0,0[245x80,0,0,241,245x20,0,81,242],244x101,246,0[244x80,246,0,243,244x20,246,81,244]}
    panes:
      - shell_command:
        - claude
      - shell_command:
        - ssh-add -D # remove all ssh identities
        - ssh-add --apple-use-keychain ~/.ssh/id_ed25519 # load only the one for KR
        - pg_ctl start -l log/postgres.log
        - git up
        - bundle install
        - corepack enable yarn
        - yarn install
        - overmind s --procfile Procfile.dev
      - shell_command:
        - nvim README.md
      - shell_command:
        - echo "hello"
