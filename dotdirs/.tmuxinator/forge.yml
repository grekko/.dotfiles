# ~/.tmuxinator/forge.yml

name: forge
root: ~/scm/stormforger.forge
on_project_start:
  - export RBENV_DIR=
  - export RBENV_VERSION=

windows:
  - Forge:
      root: ~/scm/stormforger.forge
      layout: 986b,490x94,0,0{89x94,0,0[89x18,0,0,12,89x75,0,19,22],400x94,90,0,16}
      panes:
        - server:
          - git up
          - vagrant up
          - ssht app.local.stormforger.net
        - git log
        - vim README.md
  - Tsung:
      root: ~/scm/stormforger.tsung_service
      layout: 986b,490x94,0,0{89x94,0,0[89x18,0,0,12,89x75,0,19,22],400x94,90,0,16}
      panes:
        - server:
          - git up
          - bundle check
          - bundle install
          - echo "TODO Figure out how to start the tsung node"
        - vipe
        - vim README.md
  - Website:
      root: ~/scm/stormforger.website
      layout: b6ca,364x99,0,0{75x99,0,0,6,288x99,76,0,16}
      panes:
        - server:
          - git up
          - bundle check; or bundle install
          - echo "noop"
        - vim README.md
  - Docs:
      root: ~/scm/stormforger.docs
      layout: b6ca,364x99,0,0{75x99,0,0,6,288x99,76,0,16}
      panes:
        - server:
          - git up
          - bundle check; or bundle install
          - echo "noop"
        - vim README.md

